import java.net.DatagramSocket;
import java.net.InetSocketAddress;

public class Pollster {

	/**
	 * Main program.
	 */
	public static void main(String[] args) throws Exception {
		if (args.length != 2)
			usage();
//		String serverhost = args[0];
//		int serverport = Integer.parseInt(args[1]);
//
//		DatagramSocket mailbox = new DatagramSocket(new InetSocketAddress(serverhost, serverport));
//		PollModel pollmodel = new PollModel();
//		ServerReaderThread reader = new ServerReaderThread(mailbox,pollmodel);

//		PollModel model = new PollModel();
//		model.addListener(new ModelListener() {
//			public void reportState(String location, FireSensorState state) {
//				new FireDialog(location, state);
//			}
//		});
		
		
//
//		ViewProxy proxy = new ViewProxy(mailbox);
//		proxy.setViewListener(model);

		PollsterUI.create();
	}


	   /**
	    * Print a usage message and exit.
	    */
	   private static void usage()
	      {
	      System.err.println ("Usage: java Pollster <pollsterhost> <pollsterport>");
	      System.exit (1);
	      }

}
